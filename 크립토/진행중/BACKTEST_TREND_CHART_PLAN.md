# 백테스트 추이 차트 — 구현 완료 + 후속 확인

> 작성일: 2026-03-21
> **구현 완료, 차트 데이터 확인 예정: 2026-03-24 이후**

---

## 구현 완료 (2026-03-21)

### 변경 파일

| 파일 | 변경 내용 |
|------|-----------|
| `types/crypto.ts` | `BacktestTrendPoint`, `BacktestCumulativePoint`, `BacktestTrendResponse` 타입 추가 |
| `app/api/crypto/backtest/route.ts` | `mode=trend` 분기 + `buildTrendResponse()` 일별 집계 (60일 제한, JS in-memory) |
| `lib/i18n.ts` | 4키 × 5언어 = 20개 번역 (trend/distribution/cumulative/needMoreData) |
| `components/crypto/BacktestTrendCharts.tsx` | **신규** — 탭 방식 3개 차트 (적중률 LineChart / 분포 BarChart stacked / 누적 AreaChart) |
| `components/crypto/BacktestReport.tsx` | 차트 통합 + ResizeObserver 기반 동적 maxHeight (기존 800px 고정 → 자동) |

### 계획 대비 변경점

| 원래 계획 | 실제 구현 | 이유 |
|-----------|-----------|------|
| 차트 3개 수직 배치 | **탭 방식** 전환 | 모바일에서 480px 수직 과도 |
| maxHeight 800→2000px | **ResizeObserver 동적 측정** | 고정값 transition 어색 |
| 기존 응답에 trend 포함 | **별도 `mode=trend` 경량 endpoint** | 이중 fetch 방지, 페이로드 분리 |
| B-1 타입 (라벨별 row) | **피벗 구조** (라벨=필드) | Recharts dataKey 호환 |

### API

```
GET /api/crypto/backtest?mode=trend&lookup_window=24h&signal_type=fomo

→ { trendData, distributionData, cumulativeData }
```

---

## 2026-03-24 이후 확인할 것

### 1. 차트 렌더링 확인

현재 평가완료 데이터가 1일치(2026-03-21)뿐이라 "데이터 축적 중" 메시지 표시 중.
3일 이상 쌓이면 차트가 자동으로 표시됨.

**확인 방법:**
- `/api/crypto/backtest?mode=trend&lookup_window=1h&signal_type=fomo` 호출 → `trendData` 배열 길이 ≥ 3 확인
- 대시보드 → 시그널 정확도 아코디언 열기 → 최근 결과 아래 탭 3개 표시되는지 확인

### 2. 확인 포인트

- [ ] 적중률 추이 탭: 라벨별 5개 라인이 구분되는지
- [ ] 시그널 분포 탭: stacked bar가 일별로 쌓이는지
- [ ] 누적 정확도 탭: area 차트가 우상향/안정화 추세인지
- [ ] lookupWindow (1h/6h/24h/7d) 전환 시 차트 refetch 정상 동작
- [ ] 모바일에서 탭 레이아웃 깨지지 않는지
- [ ] 아코디언 열림/닫힘 transition 자연스러운지

### 3. 데이터 충분히 쌓였을 때 판단

- **1주일 후 (2026-03-28)**: sanitizeText 배포 효과 + 가격 데이터 축적 → 24h/7d lookup window도 의미 있는 차트 가능
- 샘플 부족 라벨(extremely_hot 등)은 일별 건수 적어 적중률 변동 클 수 있음 → 정상

---

## KG 비례 스코어링 반영 (2026-03-21)

KG boost가 binary → 비례로 변경됨. 백테스트 데이터 축적 후 확인할 것:

- [ ] KG boost 있는 시그널 vs 없는 시그널의 적중률 차이 (kg_boost 필드 존재 여부로 분리)
- [ ] entity confidence 높은 코인의 시그널이 실제로 더 정확한지
- [ ] narrative momentum 양방향(+/-)이 가격 방향과 상관있는지
- [ ] 비례 스코어링 전후 시그널 분포 변화 모니터링 (극단적 변화 없어야 함)

→ 유의미한 차이가 없으면 KG boost 축소/제거 고려. 차이가 있으면 가중치 튜닝.

---

## 미커밋 상태 (sanitizeText 등)

sanitizeText 수정 + 채널 관리 + 크롤러 안정성 개선은 **별도 커밋/배포 필요** (이전 세션 작업).
배포되어야 데이터 수집량 ~10배 증가 → 차트 유의미해짐.
